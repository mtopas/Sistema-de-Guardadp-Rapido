# ── API (Windows) — --host 0.0.0.0 para que el homelab llegue por 192.168.137.1 ──
# (SGR_HOST en .env NO alcanza: uvicorn CLI ignora eso; hace falta --host)
.\venv\Scripts\activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8765

# ── Frontend ──
cd frontend
npm run dev

# ── Bot local (opcional; si usás bot en Docker, no hace falta) ──
cd ..
python mybot/bot.py

# Bot en homelab + DB acá: ver HOMELAB.md (docker-compose.bot-only.yml)
