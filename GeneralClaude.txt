Summary
Blocking bugs fixed:

SGR backend wouldn't start on Windows — a debug print() in app/db/database.py used a Unicode minus sign (U+2212) that crashes on Windows' default console encoding (cp1252), killing FastAPI's lifespan before any route (including /jarvis/*) came up. Not a Jarvis bug, but it blocked testing everything.
Frontend wouldn't start clean — @tanstack/react-virtual was declared in package.json but missing from node_modules. Fixed with npm install.
Real Jarvis bugs fixed:
3. call_reason() only fell back to local mode on budget exhaustion, not on any other external-call failure (missing API key, no network, provider down). Since this environment has no OPENAI_API_KEY, every query 500'd instead of using the local Ollama fallback that's already wired up. Now it falls back to [modo local] on any external failure.
4. The "Capturar" button did nothing on /jarvis — TopBar.jsx's CTA action map never included openJarvisCapture, so clicking it silently no-op'd.
5. Chat source citations always showed "—" — the frontend read content_raw/title fields the backend never sends; it actually sends title_hint.

Validated working (via live tests + mocked-LLM pipeline runs): capture → worker processing → vault write → embedding → RAG retrieval → privacy filtering → conversational history → query response; Privacy Gateway secret-blocking; budget EXHAUSTED state; migration dry-run idempotency against your real Bóveda backup.

I cleaned all QA test data out of jarvis.db/vault/chroma so it's genuinely empty for first real use, and updated Cerebro/estado-actual.md, Cerebro/decisiones-implementacion.md, and a new Jarvis_0.1_Pruebas.md with everything I couldn't test here (real Ollama, real OpenAI key, an actual browser, and Telegram — plus a flagged-but-unconfirmed Markdown-escaping risk in /jq's Telegram replies).

---

**Recomendación para vos**: agregar un techo de versión a `litellm` en `jarvis/pyproject.toml`
(hoy es `litellm>=1.40.0`, sin límite superior) para que un `pip install` futuro no vuelva a traer una
versión incompatible con Python 3.10, y considerar loguear la excepción real en el
`except ImportError` de `project/app/main.py` (línea ~141) en vez de tragarla en silencio — así un
futuro "no anda `/jarvis/*`" se diagnostica en segundos en vez de con este mismo proceso de
investigación.

---

Frontend — polling automático confirmado en vivo; falta interacción manual en el navegador

Con el backend + worker reales corriendo, el panel de `/jarvis` estuvo pegándole solo a
`/jarvis/inbox` y `/jarvis/budget` cada 15s durante las pruebas del punto 1 — el auto-refresh funciona.
Los dos bugs de UI que encontré (botón "Capturar" del TopBar sin wiring, fuentes del chat mostrando
"—" en vez del `title_hint`) están corregidos en código y el dato que faltaba ya se confirmó correcto
por API, pero ninguno de los dos se verificó todavía haciendo click de verdad en el navegador — junto
con el resto de la interacción manual (mandar preguntas desde la UI, F5 con historial, ciclar módulos,
colores de la barra de budget) queda para que lo pruebes vos.

---

Flujo real con Ollama corriendo — PROBADO en vivo, funciona bien (2026-08-25, esta máquina)

Corrido de punta a punta contra Ollama real (`llama3.2:3b` + `nomic-embed-text`), sin mocks:
captura → clasificación → vault `.md` → embedding en ChromaDB → `DONE` → consulta RAG con historial →
fallback `[modo local]` cuando falta `OPENAI_API_KEY` (el fix de este QA, confirmado en vivo) →
`sources` con `title_hint` correcto. Todo funcionó como se esperaba.

Hubo que resolver dos bloqueantes de entorno primero (no relacionados con Jarvis en sí, ver detalle en
`decisiones-implementacion.md` 2026-08-25 "Fijar litellm==1.60.2 e instalar jarvis editable"):
`litellm` no estaba instalado (se fijó a `1.60.2` por incompatibilidad de versiones nuevas con Python
3.10) y el paquete `jarvis` no estaba instalado en `project/venv` (montaba el backend sin rutas
`/jarvis/*`, en silencio). Recomendación pendiente: poner techo de versión a `litellm` en
`jarvis/pyproject.toml` y loguear el `except ImportError` de `app/main.py` en vez de tragarlo en
silencio.

Nota de tooling (no es bug de Jarvis): probando desde PowerShell 5.1, tildes/ñ se corrompen tanto al
mandar como al recibir JSON por defecto (`Invoke-RestMethod`/`Invoke-WebRequest` no asumen UTF-8) — hay
que forzar el encoding manualmente en ambas direcciones. El frontend real no tiene este problema.

**Quedó pendiente**: una entrada de prueba (`entry_id=23cddf94-…`) no se pudo borrar de `jarvis.db`
desde esta sesión (bloqueado por el permission classifier del harness) — inofensiva, pero si querés la
base perfectamente limpia borrala a mano de `memory_entries` e `inbox_queue`.

---

Bot de Telegram real — PROBADO (2026-08-25, esta máquina, con tu TELEGRAM_BOT_TOKEN)

`/j` y `/jq` probados de punta a punta contra un chat real. Los dos andan bien. Detalle completo de
todo lo que sigue en `Cerebro/decisiones-implementacion.md` (entradas del 2026-08-25).

- El bot del homelab usa el mismo token que este Windows — hay que pausarlo
  (`sudo docker-compose stop bot`) para probar acá, y reactivarlo después.
- Corregidos dos bugs reales de LiteLLM+Ollama que aparecían recién a partir de la 2ª consulta en el
  mismo proceso: el retrieval se degradaba en silencio a búsqueda por palabras (perdía el contexto
  real) por un cliente HTTP interno de LiteLLM mal reusado entre llamadas, y el modelo local alucinaba
  turnos de conversación fantasma que se retroalimentaban entre consultas. Ya arreglados y verificados
  en vivo.
- La sospecha original (Markdown roto por `_` sueltos en la respuesta) no se reprodujo — Telegram lo
  toleró bien incluso con código Python lleno de guiones bajos.

---

Migración real de la Bóveda — HECHA (2026-08-25)

Antes de migrar se borraron todos los datos de prueba: filas de `jarvis.db` (`memory_entries`,
`inbox_queue`, `conversations`, etc. — se mantuvo el schema), los `.md` del vault, y solo la colección
`jarvis_memory` de ChromaDB (esa carpeta la comparte con el buscador semántico propio de la Bóveda de
SGR, colección `hojas` — se dejó intacta a propósito, no se tocó).

Corrida la migración real (`python -m jarvis.cli.migrate_boveda`, sin `--dry-run`): backup automático
de `jarvis.db` creado antes de escribir (`jarvis.bak-20260825-201528.db`), **10/10 hojas migradas, 0
omitidas** — mismo resultado que los dos dry-run previos. Cada una quedó con su `memory_entry` tipo
`SEMANTIC`, su embedding en ChromaDB, y su `.md` en `vault/SEMANTIC/`. Verificado: 10 en SQLite, 10
embebidas, 10 en Chroma, 10 archivos en el vault.

## 6. Estado de la base de datos

`project/database/jarvis.db` tiene ahora las 10 hojas de tu Bóveda migradas — es tu base real en uso,
ya no está vacía. A partir de acá, cualquier `/j`/captura nueva se suma a esto.
